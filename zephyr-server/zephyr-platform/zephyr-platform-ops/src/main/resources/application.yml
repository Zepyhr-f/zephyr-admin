server:
  port: 9811

spring:
  application:
    name: zephyr-ops

  cloud:
    nacos:
      discovery:
        server-addr: 115.190.86.189:8899
      config:
        server-addr: 115.190.86.189:8899
        file-extension: yml,yaml
        namespace: public
        group: DEFAULT_GROUP

  config:
    import:
      - optional:nacos:application.yaml?group=DEFAULT_GROUP&refreshEnabled=true

  main:
    allow-bean-definition-overriding: true

