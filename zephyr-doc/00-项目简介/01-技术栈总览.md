# 技术栈总览

> 本文档面向开发者，完整描述 Zephyr Admin 的全栈技术选型，涵盖前端、后端、基础设施三大层次，并说明每项技术的选型理由。

---

## 1. 前端技术栈（Frontend Stack）

| 分类 | 技术 / 库 | 版本 | 说明 |
|---|---|---|---|
| **核心框架** | React | 19.x | 最新并发特性，支持 Suspense 流式渲染 |
| **构建工具** | Vite | 6.x | 极速 HMR，ESM 原生支持 |
| **语言** | TypeScript | 5.x 严格模式 | 全量类型安全，启用 `strict: true` |
| **UI 组件库** | Ant Design | 5.x | 企业级 React 组件，Design Token 主题定制 |
| **路由** | React Router | 7.x | 数据路由（Data Router），支持 loader/action |
| **状态管理** | Zustand | 4.x | 轻量原子化状态，替代 Redux 臃肿模板 |
| **服务端状态** | TanStack Query | 5.x | 接口数据缓存、后台同步、分页/无限加载 |
| **HTTP 客户端** | Axios | 1.x | 请求拦截、统一错误处理、Token 注入 |
| **表单处理** | React Hook Form + Zod | 7.x / 3.x | 非受控高性能表单 + Schema 声明式验证 |
| **样式方案** | TailwindCSS v4 + vanilla-extract | 4.x / 1.x | 原子化工具类 + CSS-in-TS 主题封装 |
| **动画** | Motion (Framer Motion) | 12.x | 声明式动画，页面切换过渡 |
| **图标** | Iconify + Lucide React | - | 统一图标体系，SVG 按需加载 |
| **图表** | ApexCharts + react-apexcharts | 4.x | 数据可视化，Dashboard 图表展示 |
| **国际化** | i18next + react-i18next | 23.x | 多语言支持（中/英） |
| **代码规范** | Biome | 2.x | 替代 ESLint + Prettier，单一工具链 |
| **Git 规范** | Lefthook + Commitlint | - | pre-commit lint 检查，commit 格式强约束 |
| **包管理** | pnpm | 10.x | 硬链接节省磁盘，workspace monorepo 支持 |
| **Mock 方案** | MSW (Mock Service Worker) | 2.x | 浏览器层 Mock，不改变请求代码 |

---

## 2. 后端技术栈（Backend Stack）

| 分类 | 技术 / 框架 | 版本 | 说明 |
|---|---|---|---|
| **开发语言** | Go | 1.23+ | 强类型，高并发，编译快 |
| **微服务框架** | go-zero | 1.8+ | 包含 API Gateway 与 RPC 治理的云原生微服务框架 |
| **注册中心** | Consul | 1.15+ | 服务注册与发现 |
| **网关** | go-zero API | - | 统一入口、路由转发、JWT 鉴权拦截器 |
| **ORM 框架** | GORM | 1.25+ | 自动建表，强大的关联操作与 SQL 构建能力 |
| **数据库** | PostgreSQL | 14+ | 关系型主数据库，支持强大的 JSONB 和索引 |
| **缓存** | Redis | 7.2 | Token 存储、角色权限热缓存、会话管理 |
| **认证鉴权** | golang-jwt | 5.2.1 | 基于 JWT 的无状态认证 |
| **服务间调用** | gRPC | - | 基于 Protobuf 的高性能跨服务通信 |
| **对象存储** | MinIO | - | 文件上传存储，本地开发用 MinIO |
| **包管理** | go mod | - | Go 官方模块依赖管理 |

---

## 3. 基础设施与 DevOps（Infrastructure）

| 分类 | 技术 | 说明 |
|---|---|---|
| **容器化** | Docker + Docker Compose | 本地服务一键启动（PostgreSQL/Redis/Consul/MinIO） |
| **本地容器方案** | OrbStack（macOS） | 替代 Docker Desktop，性能更好，资源占用低 |
| **反向代理** | Nginx | 前端静态文件服务，API 代理转发 |
| **CI/CD** | GitHub Actions / GitLab CI | 自动化测试、构建、部署流水线 |
| **版本控制** | Git + GitHub | 主干开发，feature branch 工作流 |

---

## 4. 技术选型决策说明

### 4.1 为什么选 React 而非 Vue？
- 生态更成熟，TypeScript 支持更完善
- 函数式编程范式，与 Hooks 天然契合
- 企业级管理系统有大量 React + Ant Design 最佳实践可参考

### 4.2 为什么用 Zustand 而非 Redux Toolkit？
- 大幅减少样板代码，API 极其简洁
- 支持 devtools、persist、immer 等中间件
- 管理系统状态复杂度适中，无需 Redux 全套方案

### 4.3 为什么用 TanStack Query？
- 接口数据缓存、自动重试、后台刷新开箱即用
- 彻底解决 useEffect + useState 管理接口状态的混乱问题
- 与 Zustand 分工明确：服务端状态用 Query，客户端 UI 状态用 Zustand

### 4.4 为什么选 Biome 而非 ESLint + Prettier？
- 单一工具，无需维护两套配置
- Rust 编写，速度是 ESLint 的 25x
- 规则集与 ESLint 高度兼容，迁移成本低

### 4.5 为什么后端使用 Go 和 go-zero 重构？
- Go 原生具有极高的并发处理能力与极低的资源开销。
- go-zero 提供了开箱即用的微服务组件、内置了 gRPC/API Gateway 生成器（goctl）。
- 部署极为简便，直接编译为无依赖二进制文件。
