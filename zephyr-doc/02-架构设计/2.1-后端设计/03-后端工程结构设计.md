# 后端工程结构设计

> 本文档规定 Zephyr Admin 后端的 Maven 多模块目录结构、各模块职责边界、代码分层约定，以及核心优化方案的落地位置，是所有后端开发者的**行为准则**。

---

## 1. 总体架构分层

```
框架基础设施层 (zephyr-framework)
    ↑ 纯技术底座：被所有服务与模块依赖（禁止引入任何业务代码）
API 契约层 (zephyr-api)
    ↑ 跨服务调用桥梁：只放 Feign 接口与 RPC DTO（禁止实现类）
平台域服务 (zephyr-platform-service)
    ↑ 后台通用管理能力：认证、系统管理、审计等（可独立进程部署）
业务域服务 (zephyr-business-service)
    ↑ 甲方核心业务：订单、商品等（可独立进程部署）
网关层 (zephyr-gateway)
    ↑ 对外唯一入口：路由转发 + 认证鉴权（粗粒度）
```

---

## 2. Maven 多模块目录结构

```
zephyr-server/
├── pom.xml                                  # 根 POM：模块关系 + BOM 引入 + 全局构建插件
│
├── zephyr-framework/                        # ①【框架基础设施】纯技术通用底座，不带任何业务
│   ├── zephyr-bom/                          #    BOM：全局三方件版本控制中心
│   ├── zephyr-core/                         #    核心基类：R<T>、异常体系、通用工具类
│   └── zephyr-starters/                     #    能力 Starter 集合（Web/MyBatis/Redis/Security）
│       ├── zephyr-starter-web/
│       ├── zephyr-starter-security/         #    JWT/双令牌、权限注解、鉴权拦截等
│       ├── zephyr-starter-mybatis/          #    MyBatis-Plus 配置、审计字段自动填充
│       └── zephyr-starter-redis/
│
├── zephyr-api/                              # ②【API 契约层】跨服务调用桥梁（只放 Feign + DTO）
│   ├── zephyr-api-system/
│   └── zephyr-api-auth/
│
├── zephyr-platform/                         # ③【平台域】后台通用管理能力（平台服务）
│   ├── zephyr-platform-service/             #    ⚡ 服务一：平台服务启动端（唯一 main）
│   ├── zephyr-platform-system/              #    系统管理：用户/角色/菜单/字典/部门（经典 MVC）
│   ├── zephyr-platform-auth/                #    认证中心：登录、Token 颁发、刷新、退出
│   └── zephyr-platform-audit/               #    审计日志：操作日志/安全日志（可异步落库）
│
├── zephyr-business/                         # ④【业务域】甲方核心业务（业务服务）
│   ├── zephyr-business-service/             #    ⚡ 服务二：业务服务启动端（唯一 main）
│   ├── zephyr-business-order/               #    订单模块（经典 MVC）
│   └── zephyr-business-product/             #    商品模块（经典 MVC）
│
├── zephyr-gateway/                          # ⑤【网关层】⚡ 服务三：唯一外部路由入口（独立进程）
│   └── src/
│       └── main/java/
│           ├── filter/
│           │   └── AuthFilter.java          #    JWT 鉴权全局过滤器（粗粒度）
│           └── config/
│               └── RouteConfig.java         #    路由配置（从 Nacos 动态加载）
│
└── scripts/                                 # 运维脚本/本地依赖
    ├── z-server.sh                          # 后端统一管理脚本
    └── docker-compose.yml                   # 本地依赖服务配置
```

---

## 3. 各模块职责详解

### 3.1 `zephyr-framework/zephyr-bom` — 依赖版本管理

**职责**：在一个地方统一锁定所有三方库版本，子模块引用依赖时**只写 groupId + artifactId，不写 version**。

**核心内容**：
- `spring-boot-dependencies`（Spring Boot BOM）
- `spring-cloud-dependencies`（Spring Cloud BOM）
- `mybatis-plus-bom`（如使用 MyBatis-Plus）
- `mapstruct`、`lombok`、`flyway` 等固定版本

---

### 3.2 `zephyr-framework/zephyr-core` — 核心工具

| 组件 | 说明 |
|---|---|
| `R<T>` | **统一响应体**，所有接口返回此结构：`{ code, success, data, msg }` |
| `ResultCode` | 全局状态码枚举（200=成功，401=未认证，403=无权限，500=系统错误） |
| `BaseEntity` | 所有业务表基类：`id`、`createTime`、`updateTime`、`createUser`、`updateUser`、`deleted` |
| `BusinessException` | 业务异常基类，携带 `ResultCode` |
| `GlobalExceptionHandler` | `@RestControllerAdvice`，统一捕获并返回 `R.fail()` |

---

### 3.3 `zephyr-framework/zephyr-starter-mybatis` — MyBatis-Plus 能力

**关键配置**：
- `MetaObjectHandler`：自动填充 `create_time`、`update_time`、`create_user`（从 `SecurityContext` 获取）
- `OptimisticLockerInnerInterceptor`：乐观锁插件，配合实体上的 `@Version` 防止并发写冲突
- `PaginationInnerInterceptor`：分页插件
- `PerformanceInterceptor`：慢 SQL 监控（开发/测试环境启用）
- 逻辑删除：全局配置 `logic-delete-field: deleted`，调用 `removeById` 自动变为 `UPDATE ... SET deleted=1`

---

### 3.4 `zephyr-framework/zephyr-starter-security` — 双令牌与鉴权能力

| 组件 | 说明 |
|---|---|
| `JwtUtil` | JWT 生成/解析，支持 ES256（默认）和 HS256 双算法切换 |
| `TokenService` | 双令牌管理：发行 Access Token（30 min）+ Refresh Token（7 天，存 Redis）|
| `BlacklistService` | Token 黑名单：将失效 `jti` 写入 Redis，网关过滤时比对 |

**双令牌流程**：
```
POST /auth/login
  └─ 验证用户名/密码（BCrypt）
       └─ 生成 Access Token（Header 返回）
       └─ 生成 Refresh Token（Set-Cookie: HttpOnly; SameSite=Strict）
       └─ Refresh Token 存入 Redis（Key: refresh:{username}, TTL: 7天）

POST /auth/refresh
  └─ 读取 Cookie 中的 Refresh Token
       └─ 比对 Redis 中存储的 Refresh Token
       └─ 有效 → 颁发新 Access Token

POST /auth/logout
  └─ 将当前 Access Token 的 jti 写入 Redis 黑名单（TTL = 剩余有效期）
  └─ 删除 Redis 中的 Refresh Token
```

---

### 3.5 `zephyr-api/*` — 跨服务契约（Feign + DTO）

**职责**：跨服务调用的桥梁，只承载“协议”，不承载“实现”。

**允许内容**：
- OpenFeign 接口（`@FeignClient`）
- RPC DTO（Request/Response/VO）、枚举、错误码（契约级）

**禁止内容**：
- `@Service`、`@Component` 等实现类
- `Mapper/Entity`、数据库访问代码
- 任何业务逻辑与调用链编排

---

### 3.6 `zephyr-platform-auth` — 认证授权模块（平台域）

| 端点 | 方法 | 说明 |
|---|---|---|
| `/auth/login` | POST | 登录，返回 Access Token + 设置 Refresh Cookie |
| `/auth/refresh` | POST | 刷新 Access Token |
| `/auth/logout` | POST | 下线，双 Token 同步失效 |
| `/auth/info` | GET | 从 Token 查询当前用户完整信息、权限列表 |

---

### 3.7 `zephyr-gateway` — 网关层

**AuthFilter 核心逻辑**：
```
请求进入
  └─ 是否白名单？（/auth/login, /auth/refresh, /actuator/**）
       ├─ 是 → 直接放行
       └─ 否 → 读取 Authorization: Bearer <token>
                  └─ JWT 签名验证
                  └─ 比对 Redis 黑名单（jti）
                  └─ URL-RBAC：按请求路径从 Redis 判断是否具备访问角色
                  └─ 通过 → 在 Header 注入 X-UserCode、X-TenantCode、X-Roles、X-Request-Id
                           → 生成 GATEWAY_SIGN_HEADER（下游据此校验网关身份）
                           → 转发
                  └─ 失败 → 返回 401
```

**设计原则**：网关负责 **身份认证（JWT 校验）+ URL-RBAC 粗粒度鉴权 + 网关签名**；细粒度权限（按钮级 perms、数据权限等）下沉到各业务服务，避免网关逻辑过重。

---

### 3.8（可选扩展）AI 能力模块归属建议

AI 相关能力若属于“全局通用平台能力”（例如全站对话、知识库、向量检索），建议归入平台域：
- `zephyr-platform-ai`（模块）+ 由 `zephyr-platform-service` 统一装配

若 AI 仅服务于某个甲方业务场景（例如订单助手、商品运营助手），建议归入业务域：
- `zephyr-business-ai`（模块）+ 由 `zephyr-business-service` 统一装配

（是否启用该模块不影响本文档的基础工程分层。）

---

## 4. 代码分层约定

业务模块代码分层约定**以现有 `system` 模块为模板**（例如 `zephyr-platform-system`），保持团队一致性。每个业务模块（未来可独立拆分为微服务）内部建议分层如下：

```
src/main/java/com/zephyr/<domain>/<module>/
├── controller/          # 接口层：只做参数校验和响应封装，无业务逻辑
├── service/             # 业务层：核心逻辑，调用 Mapper 和外部服务
│   └── impl/
├── mapper/              # 持久层：MyBatis-Plus Mapper 接口
├── entity/              # 数据库实体：继承 BaseEntity
├── domain/              # 领域对象（按 system 现有约定）
│   ├── dto/             # DTO：用于接收/传输（含查询条件、命令对象等）
│   └── vo/              # VO：用于返回给前端展示
├── convert/             # MapStruct 转换器（Entity ↔ DTO/VO）
├── util/                # 模块内部工具类（仅限本模块复用）
└── config/              # 模块内配置类（仅限本模块）
```

**命名规范**：
| 文件类型 | 命名 | 示例 |
|---|---|---|
| Controller | `XxxController` | `UserController` |
| Service 接口 | `IXxxService` | `IUserService` |
| Service 实现 | `XxxServiceImpl` | `UserServiceImpl` |
| Mapper | `XxxMapper` | `UserMapper` |
| 实体 | `Xxx` | `SysUser` |
| DTO | `XxxDTO` | `UserCreateDTO` |
| VO | `XxxVO` | `UserVO` |
| MapStruct 转换 | `XxxConvert` | `UserConvert` |

---

## 5. API 文档规范（Smart-Doc）

使用 Smart-Doc，在 `pom.xml` 中配置 Maven 插件，基于标准 JavaDoc 自动生成文档：

```xml
<plugin>
    <groupId>com.ly.smart-doc</groupId>
    <artifactId>smart-doc-maven-plugin</artifactId>
    <version>3.x</version>
    <configuration>
        <configFile>./src/main/resources/smart-doc.json</configFile>
    </configuration>
</plugin>
```

**规范**：
- 所有 Controller 方法必须有 JavaDoc 注释（`/** */`），描述接口功能
- 所有 DTO 字段必须有 JavaDoc 注释，描述字段含义与约束
- CI 中执行 `mvn smart-doc:html` 自动发布文档，无需手动维护

---

## 6. 落地优先级

| 阶段 | 内容 | 文件 |
|---|---|---|
| **S1（地基）** | `zephyr-framework`（BOM/Core/Starters）建立 + 目录分层搭建 | `pom.xml` |
| **S2（效率）** | MapStruct 接入 + 统一异常处理 + Smart-Doc | `zephyr-core`、`convert/` |
| **S3（性能）** | 虚拟线程开启 + HikariCP 调优 | `application.yml` |
| **S4（安全）** | 双令牌机制 + Redis 黑名单 + 网关鉴权落地 | `zephyr-starter-security`、`zephyr-gateway` |
| **S5（扩展）** | AI 能力接入（按归属落到 platform/business） | `zephyr-platform-ai` 或 `zephyr-business-ai` |
