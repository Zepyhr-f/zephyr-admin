# 任务目标
请帮我修改前端基础骨架（Layout）

# 视觉与结构需求说明（请严格按照以下细节实现）

## 1. 整体布局 (基于 ProLayout 核心配置)
- **左侧菜单 (Sider)**：使用 `splitMenus` 风格（双列菜单）。
  - 第一列是图标加模块名称 图标在上，名称在下的窄边栏（如：仪表盘、模板中心、系统管理等）。
  - 第二列是展开的二级菜单（如：用户管理、角色管理、部门管理等）。
  - 顶部 Logo 区显示品牌名称："Zephyr Admin"。
- **顶部导航 (Header)**：
  - 左侧：渲染面包屑（Breadcrumb），当前显示为 "系统管理 / 用户管理"。
  - 右侧（RightContent）：依次包含多语言切换、设置图标、暗黑模式切换（月亮图标），以及最右侧的用户头像。

## 2. 多标签页导航 (Tabs / KeepAlive)
- 在顶部 Header 的正下方，需要实现一个多页面标签栏（类似浏览器的 Tabs）。
- 可打开多个标签卡片（如：标签面板、岗位管理、角色管理、用户管理、聊天）。标签卡需要有关闭 "x" 按钮。

## 3. 核心内容区 (基于 PageContainer 和 ProTable)
当前页面是“用户管理”，请使用 `<ProTable />` 组件一次性实现以下所有功能：
- **顶部搜索区 (Search Form)**：
  - 包含多个输入框：用户名等。
  - 右侧操作区包含：“重置”按钮（默认态）、“查询”按钮（Primary 蓝色）和“展开/收起”按钮。
- **表格工具条 (Toolbar)**：
  - 左侧（actions）：一个的“新增用户”主要按钮。
  - 右侧（settings）：包含标准的刷新、斑马纹/密度切换、全屏、列设置等图标。
- **数据表格列定义 (Columns)**：
  - 现有数据列：
  - 最后一列固定为“操作”列，包含两个浅蓝/浅红背景的图标按钮：“编辑”图标和“删除（垃圾桶）”图标。
- **分页器 (Pagination)**：
  - 位于表格左下方/右下方，显示“共 14 条”，支持切换每页条数（当前显示 20条/页），支持跳页。


# 代码输出要求
1. 请给出核心的文件目录结构规划（如 `src/app.tsx`, `src/layouts`, `src/pages/System/User`）。
2. 提供 `ProTable` 的完整配置代码，包括 `columns` 定义、`request` 模拟数据加载以及 `toolBarRender` 配置。
3. 代码请保证高度的模块化和类型安全（TypeScript）。
