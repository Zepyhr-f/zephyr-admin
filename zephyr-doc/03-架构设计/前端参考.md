import { AvatarDropdown, AvatarName } from './components/RightContent/AvatarDropdown';
import { SearchOutlined, BellOutlined, SettingOutlined, MoonOutlined, FullscreenOutlined } from '@ant-design/icons';
import { Space } from 'antd';
import type { RunTimeLayoutConfig } from '@umijs/max';

// 配置 ProLayout
export const layout: RunTimeLayoutConfig = ({ initialState }) => {
  return {
    title: 'Art Design Pro X',
    logo: 'https://gw.alipayobjects.com/zos/rmsportal/KDpgvguMpGfqaHPjicRK.svg',
    // 开启双列菜单模式 (对应图中的 Sider 结构)
    layout: 'mix',
    splitMenus: true, 
    // 顶部右侧的操作区 (对应图右上角的图标)
    rightContentRender: () => (
      <Space size="middle" style={{ marginRight: 24 }}>
        <SearchOutlined style={{ cursor: 'pointer', fontSize: 16 }} />
        <FullscreenOutlined style={{ cursor: 'pointer', fontSize: 16 }} />
        <BellOutlined style={{ cursor: 'pointer', fontSize: 16 }} />
        <SettingOutlined style={{ cursor: 'pointer', fontSize: 16 }} />
        <MoonOutlined style={{ cursor: 'pointer', fontSize: 16 }} />
        <AvatarDropdown>
          <AvatarName />
        </AvatarDropdown>
      </Space>
    ),
    // 模拟多标签页导航 (真实项目中通常配合 keep-alive 插件实现)
    childrenRender: (children) => {
      return (
        <>
          {/* 这里预留给 Tabs 插件渲染的位置 */}
          <div style={{ background: '#fff', padding: '8px 24px', borderBottom: '1px solid #f0f0f0' }}>
            <Space>
              <span style={{ padding: '4px 12px', background: '#e6f4ff', color: '#1677ff', borderRadius: 4 }}>用户管理 x</span>
              <span style={{ padding: '4px 12px', color: '#666' }}>角色管理 x</span>
            </Space>
          </div>
          {children}
        </>
      );
    },
  };
};